{
  "name": "@promptlint/config",
  "version": "1.0.0",
  "description": "Configuration loader for `promptlint.config.{ts,json}`. Validates with Zod and exposes rule-severity and rule-option overrides.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist .turbo coverage",
    "topo": "echo \"topo\""
  },
  "dependencies": {
    "@promptlint/types": "workspace:*",
    "picomatch": "2.3.2",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@promptlint/biome-config": "workspace:*",
    "@promptlint/tsconfig": "workspace:*",
    "@types/node": "20.17.10",
    "@types/picomatch": "4.0.3",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
