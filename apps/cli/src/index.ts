/**
 * Phase 0 placeholder for the PromptLint CLI.
 *
 * The full command-line interface (argument parsing, config loading,
 * rule dispatch, reporter selection) is implemented in Phase 2.
 * This file intentionally exports nothing so downstream packages
 * cannot accidentally depend on unstable surface area.
 */
export {}
