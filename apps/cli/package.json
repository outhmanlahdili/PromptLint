{
  "name": "@prompt-lint/cli",
  "version": "1.0.0",
  "description": "PromptLint command-line interface. Lint your prompts. Catch regressions before your users do.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "bin": {
    "promptlint": "./bin/promptlint.ts"
  },
  "scripts": {
    "lint": "biome check src bin",
    "lint:fix": "biome check --write src bin",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "build": "echo \"no-op (CLI runs from source via tsx)\" && exit 0",
    "clean": "rm -rf dist .turbo coverage",
    "topo": "echo \"topo\""
  },
  "dependencies": {
    "@prompt-lint/config": "workspace:*",
    "@prompt-lint/parser": "workspace:*",
    "@prompt-lint/reporter-human": "workspace:*",
    "@prompt-lint/reporter-json": "workspace:*",
    "@prompt-lint/rule-engine": "workspace:*",
    "@prompt-lint/rules": "workspace:*",
    "@prompt-lint/types": "workspace:*",
    "tsx": "4.19.2"
  },
  "devDependencies": {
    "@prompt-lint/biome-config": "workspace:*",
    "@prompt-lint/tsconfig": "workspace:*",
    "@types/node": "20.17.10",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
